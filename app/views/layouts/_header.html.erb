<!--<div class="grid_12" >
  <ul id="projnav" style="font-variant:small-caps;">
    <li>
      <%#= link_to_unless_current(t('about'), about_path) do
      link_to(t('about'), activities_site_path, :style=>"color:#fd039c;")
      end %>
    </li>
    <li>
      <%#= link_to_unless_current(t('news'), posts_path) do
      link_to(t('news'), posts_path, :style=>"color:#fd039c;")
      end %>
    </li>
     <li>
      <%#= link_to_unless_current(t('events'), events_site_path) do
      link_to(t('events'), events_site_path, :style=>"color:#fd039c;")
      end %>
    </li>
    <li>
      <%#= link_to_unless_current(t('contact'), photos_site_path) do
      link_to(t('contact'), photos_site_path, :style=>"color:#fd039c;")
      end %>
    </li>
  </ul>
</div>

<div class="clear"></div>-->

<%= tabs_tag :open_tabs => { :id=> "projnav" } do |tab| %>
  <%= tab.about  'About', about_site_path%>
  <%= tab.news 'News', posts_path %>
  <%= tab.events 'Events', events_site_path %>
  <%= tab.photos 'Photos', photos_site_path %>
<% end %>



